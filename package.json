{
  "name": "rifas",
  "version": "0.1.0",
  "private": true,
  "description": "Raffle management system for organizers",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "db:generate": "pnpm --filter @rifas/api db:generate",
    "db:migrate": "pnpm --filter @rifas/api db:migrate",
    "db:studio": "pnpm --filter @rifas/api db:studio",
    "dev": "pnpm --filter @rifas/api dev",
    "dev:web": "pnpm --filter @rifas/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:int": "pnpm --filter @rifas/api test:int",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@11.10.0"
}
