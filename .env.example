# Infrastructure only: consumed by docker-compose.yml.
# Application settings live in backend/.env and frontend/.env.
POSTGRES_USER=rifas
POSTGRES_PASSWORD=rifas_dev
POSTGRES_DB=rifas
# Non-default port: 5432 is often taken by a host PostgreSQL install.
POSTGRES_PORT=5434
ADMINER_PORT=8080
